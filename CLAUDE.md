# RealFi Office Hours アーカイブ

録音の文字起こしから、1 回につき 3 ページの HTML を生成するリポジトリ。

## 新しい回を追加するときの手順

文字起こしを渡されたら、以下を順に行う。

### 1. 原本を保存する

```
episodes/YYYY-MM-DD-<slug>/
├── meta.yml
└── transcript.txt   ← 渡されたテキストを一切加工せずそのまま保存
```

`transcript.txt` は**絶対に編集しない**。誤字も改行もそのまま。ここが再生成のもとになる。

`meta.yml` には日付・タイトル・プラットフォーム・話者・質問者・参加人数を記録する。日付が文字起こしから特定できない場合はコメントで「要確認」と明記する。

### 2. `shared/` を読む

生成の前に必ず 3 つとも読む。

- `shared/glossary.md` — 用語の統一表記、音声認識の誤変換の修正表、翻訳方針
- `shared/people.md` — 話者と質問者の表記ゆれ、前回までの話者推定の根拠
- `shared/open-questions.md` — 過去の回で保留になった質問。**今回の回で回答されているものがないか確認する**

### 3. 3 つの HTML を生成する

```
docs/YYYY-MM-DD-<slug>/
├── index.html          ← メイン。日本語のわかりやすいまとめ
├── transcript-ja.html  ← 日本語全文
└── transcript-en.html  ← 英語全文（原文のまま）
```

3 ページとも先頭に `.docnav` を置き、互いにリンクする。`../assets/style.css` を参照する。

**index.html（まとめ）の構成**

1. `.tldr` — 要点 5 つ前後。これだけ読めば概要が分かる粒度
2. 登壇者
3. 本編を話題ごとに再構成した見出し（時系列に縛られない。同じ話題が離れて出てくる場合はまとめる）
4. Q&A — 質問者名つきで `.qa` カードにする
5. 持ち帰りになった質問 — `.qa.pending` にする
6. 末尾に `.note` で「話者は推定」「原文は英語全文を参照」と明記する

**transcript-en.html（英語全文）**

- 話者を推定して `.turn` に分割する。**文言は一切変えない**。誤変換もそのまま残す
- 推定が不確かな話者には `<em>(inferred)</em>` を付ける
- 段落の区切りは原文に従う
- 話題の切れ目に `.sec` を挿入する

**transcript-ja.html（日本語全文）**

- 英語全文と同じ `.turn` 分割・同じ `.sec` 区切りにする（対応が取れるように）
- 誤変換は `glossary.md` に従って修正する
- 不確かな話者には `<em>(推定)</em>` を付ける

### 4. `shared/` を更新する

- 新しい用語・新しい誤変換パターンが出たら `glossary.md` に追記
- 新しい質問者・話者が出たら `people.md` に追記
- 今回保留になった質問を `open-questions.md` に追記。過去の質問が回答されていたらステータスを更新

### 5. `docs/index.html` に 1 枚カードを追加する

新しい回が上に来るように、既存のカードより前に挿入する。

## 書き方のルール

- **話者ラベルもタイムスタンプもない文字起こしが前提**。話者は文脈から推定する。推定であることを必ずページ上に明記し、根拠の弱い箇所は `people.md` に記録する
- **断定していない発言は日本語でも断定しない**。"I think"、"probably"、"we haven't landed on" は、ロードマップや時期の話で特に重要
- **プロダクト用語は訳さない**（USDR, sUSDR, Cardano, EVM, DEX, SDK, testnet, mainnet など）
- 口語のフィラー（you know, I mean, like）は日本語訳では落とす
- 発言者が誰かを本文で示すときは、まとめページでは太字ではなく `.qa-a` の `data-by` 属性を使う
- **原文にない情報を足さない**。補足が必要なら `.note` に入れて、まとめ本文と区別する

## 公開

`docs/` を GitHub Pages のルートに設定すれば公開できる。`episodes/` は公開されないので、生の文字起こしがそのまま出ることはない。

## ディレクトリ

```
├── CLAUDE.md
├── README.md
├── episodes/          原本（文字起こし・メタ情報）。触らない
├── shared/            回をまたぐ資産（用語集・人物・持ち帰り質問）
└── docs/              公開する成果物（GitHub Pages のルート）
    ├── index.html     全回の一覧
    ├── assets/style.css
    └── YYYY-MM-DD-<slug>/
```
