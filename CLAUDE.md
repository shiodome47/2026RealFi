# Cardano セッションのアーカイブ

録音・録画の文字起こしから、1 回につき 3 ページの HTML を生成するリポジトリ。

## 最優先のルール：まず、その回が匿名化の対象かを決める

**回によって扱いが違う。**新しい文字起こしを渡されたら、最初にどちらかを確定させる。

| 種別 | `meta.yml` | 扱い |
| --- | --- | --- |
| **RealFi Office Hours** などのコミュニティコール | `chatham_house_rule: true` | **チャタムハウスルール。**内容は共有してよいが、**誰が発言したかは書かない** |
| **公開されている録画**（YouTube など） | `chatham_house_rule: false` | **実名のまま扱う。**固有名詞の明らかな誤変換は修正する |

### チャタムハウスルールの回

このリポジトリは **Public** なので、`episodes/` に保存する文字起こしも含めて、**実名・ハンドル名・ステークプール名をどのファイルにも残さない**。

置換のルールとチェックリストは `shared/speakers.md` にある。**生成の最後に必ず残存チェックを行う。**

新しい名前が出たら `tools/check.py` の `NAMES` に**その回の slug の下に**足す。`NAMES` は回ごとの辞書で、名前は回をまたぐと普通の単語や別人の名前になりうるため、**全体を一括で検索してはいけない**。

### 公開録画の回

実名を残すので `NAMES` には足さない。代わりに次をやる。

- `meta.yml` に `chatham_house_rule: false` と、公式チャンネルかどうかを確認したかを書く。**確認していないなら「公式チャンネルの配信かどうかは確認していない」と明記し、番組名と出演者以外の出典情報は書かない**
- **固有名詞の誤変換を修正する。**修正表は `shared/glossary.md` に回ごとに追記する。**特定できなかった名前は聞こえたまま残し、まとめページで「名前だけ挙がって説明されなかった」と事実として書く**
- 全文ページの断り書きを「匿名化以外は未編集」ではなく「固有名詞の誤変換を修正済み」にする
- まとめページの先頭に `.note` を置き、**この回はチャタムハウスルールの対象ではないこと**を明示する

`docs/index.html` のカードは**種別ごとにまとめる**。ディレクトリ構成は平置きのまま変えない（既存の URL を壊さないため）。

## 新しい回を追加するときの手順

### 1. 原本を保存する（チャタムハウスルールの回は匿名化してから）

渡された文字起こしから実名を落とし、ラベルに置き換えてから保存する。

```
episodes/YYYY-MM-DD-<slug>/
├── meta.yml
└── transcript.txt   ← 匿名化した以外は無加工
```

匿名化以外の加工はしない（誤字も改行もそのまま）。**公開録画の回では匿名化もしないので、渡されたテキストをそのまま置く。**ここが再生成のもとになる。固有名詞の修正は `docs/` 側でだけ行う。

チャタムハウスルールの回では、置換を次の 2 段階で行う。

1. 話者・参加者を `進行役` / `登壇者A・B・C` / `参加者A・B・C…` に寄せる（登場順に振る）
2. **発言の中で呼びかけられた名前**も置換する（"Thanks, ○○"、"○○, do you want to add?" など）。ここが一番漏れやすい

`meta.yml` には日付・タイトル・プラットフォーム・参加人数を記録する。日付が文字起こしから特定できない場合はコメントで「要確認」と明記する。

### 2. `shared/` を読む

生成の前に必ず 3 つとも読む。

- `shared/glossary.md` — 用語の統一表記、音声認識の誤変換の修正表、翻訳方針
- `shared/speakers.md` — 匿名化のルール、ラベルの振り方、過去回の話者推定の確度
- `shared/open-questions.md` — 過去の回で保留になった質問。**今回の回で回答されているものがないか確認する**

### 3. 3 つの HTML を生成する

```
docs/YYYY-MM-DD-<slug>/
├── index.html          ← メイン。日本語のわかりやすいまとめ
├── transcript-ja.html  ← 日本語全文
└── transcript-en.html  ← 英語全文（匿名化／固有名詞の誤変換の修正以外は原文のまま）
```

3 ページとも先頭に `.docnav` を置き、互いにリンクする。`../assets/style.css` を参照する。

**index.html（まとめ）の構成**

1. `.thesis` — **「一言でいうと」**。この回が結局何の話だったのかを 1〜2 文で。ここが一番大事
2. `.tldr` — 要点 5 つ前後
3. `.note` で扱いの断り（チャタムハウスルールの回はその旨、公開録画の回は**対象外である**旨）
4. 本編を話題ごとに再構成した見出し（時系列に縛られない。同じ話題が離れて出てくる場合はまとめる）
5. **全体像** — 話がどう組み合わさるのかを示す図で締める
6. Q&A — 質問者ラベルつきで `.qa` カードにする
7. 持ち帰りになった質問 — `.qa.pending` にする
8. 末尾に `.note` で断り書き

**transcript-en.html（英語全文）**

- 話者を推定して `.turn` に分割する。**匿名化（と、公開録画の回では固有名詞の誤変換の修正）以外は文言を変えない**。それ以外の誤変換はそのまま残す
- 同じ話者の独白が長く続く回では、**話題の切れ目で `.turn` を分けてよい**。日本語全文と同じ分割にすること
- 推定が不確かな話者には `<em>(inferred)</em>` を付ける
- 段落の区切りは原文に従う。話題の切れ目に `.sec` を挿入する

**transcript-ja.html（日本語全文）**

- 英語全文と**同じ `.turn` 分割・同じ `.sec` 区切り**にする。生成後に両者のブロック数が一致することを確認する
- 誤変換は `glossary.md` に従って修正する
- 不確かな話者には `<em>(推定)</em>` を付ける

### 4. 図解を入れる

**まとめページには図を入れる。**文章だけで説明しきれる話でも、構造・順序・時間変化があるものは図にしたほうが速く伝わる。

インライン SVG で書き、`.fig` でラップして `.figcap` を付ける。外部ファイルや画像は使わない。

```html
<div class="fig">
<svg viewBox="0 0 680 250" role="img" aria-label="図の内容の説明">
  ...
</svg>
<p class="figcap">図の補足</p>
</div>
```

- 色は直接指定せず、用意済みのクラスを使う（`d-box` `d-box-accent` `d-box-warn` `d-box-plain` / `d-t` `d-s` `d-a` `d-w` / `d-line` `d-line-accent` / `d-head` `d-head-accent`）。ライト・ダーク両テーマに自動で追従する
- `viewBox` の幅は **680** に揃える。高さは内容次第
- 矢印マーカーの `id` は図ごとに一意にする（`m1` `m2` …）。同じページに複数の図があると衝突する
- `role="img"` と `aria-label` を必ず付ける

**図にすると効くもの** — 構成図（どこが中心か）、フロー図（何がどの順で起きるか）、対比図（現在と将来、通常と改善後）、数値例（時間経過で値がどう変わるか）。

### 5-a. 専門用語が密な節は折りたたみで解説する

本文を長くすると読み通せなくなるが、用語が分からないと節ごと飛ばされる。**本文は短いまま、`<details class="explain">` に「かみくだいた説明」を入れる**。

```html
<details class="explain">
<summary>専門用語が多い節です — かみくだいた説明を開く</summary>
<div class="explain-body">
  <p><span class="badge">補足</span>以下はこちらで足した解説です。コール中の発言ではありません。</p>
  <h4>そもそも「◯◯」とは</h4>
  ...
</div>
</details>
```

書き方 — ①用語そのものの意味 → ②なぜそれが要るのか → ③**身近なものへのたとえ** → ④一言でまとめ、の順。たとえ話は 1 つに絞る。**中身は必ず `補足` 扱いにする。**

**「このコールでは分からないこと」も書く。**説明が途中で止まっている箇所は、止まっていること自体が読者にとって重要な情報になる。`shared/open-questions.md` にも記録する。

### 5. コール中の発言でない補足には印を付ける

理解を助けるために説明を足すのはよいが、**コールで言われたことと混ぜない**。足した内容には `<span class="badge">補足</span>` を付ける。

例：リスクの指摘、仕組みを説明するための数値例、一般的な背景知識。

### 6. `shared/` を更新する

- 新しい用語・誤変換パターンが出たら `glossary.md` に追記
- 今回のラベルの振り方と、推定が不確かだった箇所を `speakers.md` に追記
- 今回保留になった質問を `open-questions.md` に追記。過去の質問が回答されていたらステータスを更新

### 7. `docs/index.html` に 1 枚カードを追加する

新しい回が上に来るように、既存のカードより前に挿入する。カードの説明文には「一言でいうと」の内容を入れる。

### 8. 完成条件（毎回チェックする）

**言われなくても毎回満たすこと。**ひとつでも欠けていたら未完成。

#### 読みやすさ

長い文章は、それだけで読む気を削ぐ。**どこに目をやればいいかが一目で分かる状態**にする。

- [ ] `.thesis`（一言でいうと）がページ先頭にある
- [ ] `.tldr`（要点 5 つ前後）がある
- [ ] `.toc`（目次）がある。節が 6 つ以上なら必須。2 列で全体の見取り図になること
- [ ] **すべての `<h2>` に `id` と `.deck`（1 行の要約）が付いている**。読むか飛ばすかを本文の前に判断できるようにするため
- [ ] 見出しが本文と明確に違う重さになっている（`h2` は本文の 1.5 倍）
- [ ] 太字を使いすぎていない。1 段落に何箇所も太字があると強調が機能しない
- [ ] 節と節の間に十分な余白がある

#### 言語

サイトは日本語と英語の両方で読める。**まとめは両言語で書く。**

- [ ] まとめページに `<div class="l-ja">` と `<div class="l-en" lang="en">` の両方がある
- [ ] `.lang-toggle` ボタンがある。初回訪問時は `navigator.language` で自動判定し、選択は `localStorage` に保存する
- [ ] **図の中の文字も英語版を用意する。**日本語の図をそのまま英語ページに出さない
- [ ] 英語版の `<h2>` の `id` は `en-` を前置し、日本語版と重複させない。SVG の `marker` の `id` も同様に分ける（日本語 `m1`…／英語 `e1`…）
- [ ] ヘッダー・ナビ・フッターの文言も `<span class="l-ja">` / `<span class="l-en">` で両方用意する
- [ ] `<title>` は両言語で通じる表記にする（CSS で切り替えられないため）
- [ ] 全文ページは単一言語なので、そのページ自身の言語でナビを固定する。言語ボタンは対になるページへのリンクにする

英語版は日本語からの逐語訳にしない。**元のコールは英語なので、原文の言い回しをそのまま引用したほうが自然になる。**

#### 配色

- [ ] **ライト・ダーク両方で成立している。**色は必ず CSS 変数（`--bg` `--text` `--accent` など）経由で指定し、直値を書かない
- [ ] `prefers-color-scheme`（OS 設定への自動追従）と `:root[data-theme]`（明示指定）の両方に対応している
- [ ] `.theme-toggle` ボタンがある。自動追従だけだと切り替え手段がなく、存在に気づけない
- [ ] 選択は `localStorage` に保存し、再訪時も維持される

#### 図解

- [ ] まとめページに図がある（`### 4. 図解を入れる` 参照）
- [ ] 図の色も CSS 変数経由で、両テーマに追従する

#### その他

- [ ] ナビ（`.docnav`）は**ページ末尾**。案内であって中身ではないので先頭に置かない。`standalone.html` のジャンプナビ（`.jump`）も同じで、**末尾にだけ置く**。1 枚に 3 つ入っていることは、気づく人だけが気づけばよい
- [ ] コール中の発言でない補足に `<span class="badge">補足</span>` が付いている
- [ ] モバイル幅（560px 以下）でレイアウトが崩れない
- [ ] `aria-label` / `role` / `:focus-visible` が付いている
- [ ] `standalone.html` を再生成した（3 ページを 1 ファイルにまとめたもの）

```
python3 tools/build_standalone.py            # 全回
python3 tools/build_standalone.py <slug>    # 1 回だけ
```

**まとめや全文を直したら必ず流す。**中身は `docs/<slug>/` の 3 ページから取り直すので、手で書き換えない。

#### 機械的な検証

コミット前に必ず流す。

```
python3 tools/check.py
```

見ているもの — 実名の残存、リンク切れ、`id` と SVG `marker` の重複、アンカーの飛び先、タグの閉じ忘れ、`deck` と目次の不足、図の `role`/`aria-label`、日英まとめの `.fig`/`.qa`/`.term`/`.deck` の数の一致、英日全文の `.turn`/`.sec` の一致、テーマトークン外の色の直書き、トグルの有無。

**チャタムハウスルールの回で新しい名前が出たら `tools/check.py` の `NAMES` に、その回の slug の下に足す。**公開録画の回は実名を残すので足さない。

## 書き方のルール

- **断定していない発言は日本語でも断定しない**。"I think"、"probably"、"we haven't landed on" は、ロードマップや時期の話で特に重要
- **プロダクト用語は訳さない**（USDR, sUSDR, Cardano, EVM, DEX, SDK, testnet, mainnet など）
- 口語のフィラー（you know, I mean, like）は日本語訳では落とす
- **原文にない情報を足さない**。足すなら `補足` バッジを付けて区別する
- **初出の概念は説明を付ける。**業界用語（flywheel, TVL, PSM など）は、読者が初めて見る前提で 1 行の説明を `補足` で足す。**言葉の指す範囲が誤解されやすいものは、何を含み何を含まないかまで書く**（例: 「DeFi 統合」はプロトコル間の接続であって、他チェーンからの送金の話ではない）
- **「文字起こしからは分からない」と書かない。**この資料は読者にとって、作った人が責任を持って確認したものとして読まれる。**制作過程の限界ではなく、コールで何が語られ何が語られなかったかを事実として書く**（×「特定できません」→ ○「この一言のみで、続けて別の話題に移っています」）。`補足` バッジは出典の区別のために残す — これは不確かさの印ではない
- **コール中で名前だけ挙がった外部の提案・プロジェクトは、背景を調べて補う。**読者が調べ直さずに済む状態にする。**数値と可決状況は必ず裏取りし、時点を明記する**（状況が変わるため）
- **触れられただけで掘り下げられなかった話題も拾う。**特に地域・コミュニティ名が挙がった箇所は、読者にとって関心が高い。**掘り下げがなかったこと自体を明記する**（推測で埋めない）
- **話者のたとえを図にするときは、たとえだと分かるようにする。**「Cardano のガバナンスは立法・司法・行政」のような国家制度への類推は、図にすると**実際の制度そのもの**に見えてしまう。図の `.figcap` に「番組で語られた比喩をそのまま描いたもの」と書き、**実際の構造と食い違う点は折りたたみで補足する**。たとえの筋が通っていることと、対応関係が正確であることは別
- **技術的な仕組みを説明するときは、成り立つ条件まで確認する。**「A なら必ず B」と書く前に反例を探す（例：ステーク資格情報が一致しても同じシードとは限らない／同じシードでもアカウントが違えば一致しない）
- **「決まった」と「決まらなかった」の間を潰さない。**投票や承認の話では、**正式に成立したのか、成立していないが判断材料として使われたのか**を区別する。Cardano の Info Action のように、閾値に届かなくても結果が実務に効く仕組みがある。出典が "ratified" と書いているのか "signalled support" と書いているのかまで見る
- **組織の顔ぶれは変わる。**「◯◯の 5 社」「構成メンバーは A・B・C」のように書くときは、**必ず時点を確認して添える**。名前に数が入っているもの（Pentad など）はとくに危ない。**収録日の時点でどうだったか**と、**今どうか**は別
- **件数を人数に読み替えない。**オンチェーンで数えられるのはアドレスや資格情報であって人ではない。1 人が複数持つ場合と、複数人が 1 つに集約される場合の両方がある
- **「観測できた数」と「実際の数」を区別する。**計測方法の制約で取りこぼしが出る指標は、取りこぼしが**どちら側に偏るか**まで書く。偏っているなら、それは誤差ではなく下限である
- **数字は「何を数えたものか」まで書く。**「アクティブウォレット 20,000」のような指標は、総数と誤読されやすい。分母・期間・定義が曖昧なまま置かない。コール中で揃っているか確認できない場合は、その旨を `補足` で明記する
- まとめページでは発言者を書かない。「〜という説明がありました」「〜と述べられました」のように受動的に書く

## 公開

`docs/` を GitHub Pages の公開元に設定する。`episodes/` と `shared/` は Pages では配信されない。

## ディレクトリ

```
├── CLAUDE.md
├── README.md
├── episodes/          原本（匿名化済みの文字起こし・メタ情報）
├── shared/            回をまたぐ資産
│   ├── glossary.md
│   ├── speakers.md         匿名化ルール・話者推定メモ
│   └── open-questions.md
└── docs/              公開する成果物（GitHub Pages のルート）
    ├── index.html     全回の一覧
    ├── assets/style.css
    └── YYYY-MM-DD-<slug>/
```
